

// create account 
const createAcctBtn = document.querySelector('#createacct')

createAcctBtn.addEventListener("submit", (evt) => {
    // evt.preventDefault();

    // alert('im a button that works');
});

// Log In 
const loginBtn = document.querySelector('#login')

loginBtn.addEventListener("submit", (evt) => {
    // evt.preventDefault();
    
    // alert('Im also a button that works')
});